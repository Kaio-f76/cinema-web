services:
  frontend:
    build:
      context: .          # raiz do frontend
      dockerfile: Dockerfile
    container_name: cinema-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VUE_APP_BACKEND_URL=http://localhost:8080/api
